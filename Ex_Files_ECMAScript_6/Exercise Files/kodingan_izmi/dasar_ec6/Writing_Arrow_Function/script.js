//cara menggunakan arrow function 

let studentList = (students) => console.log(students);

studentList(["A", "B", "C"]);

let list = ["apple", "banana", "strawberry"];
list.map((item) => console.log(item));

let studentScore = (students) => console.log(students);
studentScore(["A", "A-", "B+", "B", "B-", "C", "D"]);

let birds = ["budgie", "lovebird", "cockatiel", "macaw", "lorikeet", "cockatoo"];
birds.map((item) => console.log(item));